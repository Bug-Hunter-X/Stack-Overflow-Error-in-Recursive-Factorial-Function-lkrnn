function foo(x: int): int {
  if (x == 0) {
    return 1;
  } else {
    return x * foo(x - 1);
  }
}

function main(): void {
  echo foo(5);
}

This code will cause a stack overflow error if x is a large number because the recursive calls will exceed the maximum call stack size.  The solution involves using iteration.